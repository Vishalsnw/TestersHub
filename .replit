modules = ["nodejs-20", "java-graalvm22.3"]
[agent]
expertMode = true

[nix]
channel = "stable-25_05"
packages = ["gradle"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Project Ready"

[[workflows.workflow]]
name = "Project Ready"
author = "agent"

[workflows.workflow.metadata]
outputType = "console"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "echo \"TestersHub Android project is ready! This is a native Android project — open it in Android Studio to build and run on a device/emulator.\" && echo \"\" && echo \"Project structure:\" && find app/src -name \"*.kt\" -o -name \"*.xml\" | head -20 && echo \"\" && echo \"To compile (syntax check), run: ./gradlew compileDebugKotlin\" && sleep infinity"
